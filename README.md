# talkdom.org

Static website for [talkDOM](https://github.com/eringen/talkdom). Built with talkDOM + Tailwind CSS.

```bash
npm install
make css       # build css (or css-prod for minified)
make serve     # localhost:3000
```
